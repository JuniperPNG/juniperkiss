# Botanical motif: re-enabled 6 September 2026

Feedback from Juniper, 5 September 2026: the tendril/bramble motif was called
**very gimmicky** and disabled via `botanical_motif_enabled: false` pending a
complete redesign (see git history for that commit).

On 6 September 2026 Juniper asked to turn it back on as-is (tagline, About
artwork, masthead logo mark) without a redesign having happened — no design
critique from the 5 September feedback has been addressed. `botanical_motif_enabled`
is `true` again in `_config.yml`.

If gimmicky-motif feedback resurfaces, revisit the 5 September critique above
rather than re-disabling by default — ask Juniper whether they want the flag
off again or an actual redesign this time.
